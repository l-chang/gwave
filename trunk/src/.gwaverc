;-*- mode: scheme -*-

(dbprint "in my experimental .gwaverc\n")

(debug-enable 'debug)
(read-enable 'positions)

(use-modules 
 (gtk gtk)
 (gtk gdk)
 (app gwave std-menus)
 (app gwave std-args)
 (app gwave std-toolbar)
 (app gwave cmds)
 (app gwave visiblewave-ops)
 (app gwave extra-menus)
)

; wavepanel type 0=original, tall, with min/max labels.  Good for analog.
;                1=short, no extra labels. Fit more panels for digital.
; 0 is the default.
; BUG: resetting it here affects the panel-creating menu items fine,
; but doesn't reset the state of the Options/DefaultPanelType radio-menu items.
(set! default-wavepanel-type 0)

(dbprint "experimental .gwaverc done\n")
